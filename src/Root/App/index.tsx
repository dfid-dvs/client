import React from 'react';
import Multiplexer from './Multiplexer';

import './styles.css';

interface State {}
interface Params {}
interface Props {}

const App = (props: Props) => (
    <Multiplexer />
);

export default App;
